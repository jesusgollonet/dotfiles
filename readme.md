# Dotfiless

## How to install

- clone this repo
- move to `~/.dotfiles` (paths are currently hardcoded)
- ```bash
cd ~
# symlink zshrc 
ln -s ~/.dotfiles/zsh/zshrc .zshrc
# install pure prompt
brew install pure 
```




